// import { fireEvent, render, screen } from '@testing-library/react';
// import InsertJob from './InsertJob';


// test('renders learn react link', () => {
//   render(<InsertJob />);
//   const linkElement = screen.getByPlaceholderText("HR Name *");
//   expect(linkElement).toBeInTheDocument();
// });
// it("renders 'Provider Name input field' ", () => {
//     render(<InsertJob />);
//      const linkElement = screen.getByRole("pname");
//      expect(linkElement).toBeInTheDocument();
//      expect(linkElement).toHaveTextContent("");
//     });

// it("renders 'role name input field' ", () => {
//   render(<InsertJob />);
//   const linkElement = screen.getByRole("rname");
//   expect(linkElement).toBeInTheDocument();
//   expect(linkElement).toHaveTextContent("");
// });

// it("renders 'role Id input field' ", () => {
//   render(<InsertJob />);
//   const linkElement = screen.getByRole("rId");
//   expect(linkElement).toBeInTheDocument();
//   expect(linkElement).toHaveTextContent("");
// });

// it("renders 'eligibilityCriteria input field' ", () => {
//   render(<InsertJob />);
//   const linkElement = screen.getByRole("eligibilityCriteria");
//   expect(linkElement).toBeInTheDocument();
//   expect(linkElement).toHaveTextContent("");
// });

// it("renders 'companyName input field' ", () => {
//   render(<InsertJob />);
//   const linkElement = screen.getByRole("companyName");
//   expect(linkElement).toBeInTheDocument();
//   expect(linkElement).toHaveTextContent("");
// });


// it('calls onSubmit when the submit button is clicked', async () => {
//   const onSubmit = jest.fn();
//   const { getByTestId } = render(<InsertJob onSubmit={onSubmit} />);

//   const submitButton = getByTestId('submit-button');
 

//   fireEvent.click(submitButton);


//   expect(onSubmit).toHaveBeenCalledTimes(0);
// });

// ************************************************************************************************************************************
// WebElement dropdown = driver.findElement(By.name("retName"));
// 		    	      dropdown.findElement(By.xpath("//option[. = '1']")).click();
 
// 		    	    }
// 		    Thread.sleep(1000);
// 		    {
 
// 		    	      WebElement dropdown = driver.findElement(By.name("prodName"));
// 		    	      dropdown.findElement(By.xpath("//option[. = 'Soap']")).click();
 
// 		    	    }
// 		    Thread.sleep(1000);
// 		    driver.findElement(By.id("reqQuantity")).sendKeys("500");
// 		    Thread.sleep(1000);
// 		    driver.findElement(By.cssSelector(".btn-success")).click();
// 		    Thread.sleep(1000);
// 	        driver.quit();





